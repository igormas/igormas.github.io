(function (angular) {
    'use strict';

    var localStorageService = [
        function () {
        }
    ];
    angular.module('twangular').service('localStorageService', localStorageService);
})(angular);